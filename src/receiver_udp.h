#ifndef RECEIVER_UDP_H_
#define RECEIVER_UDP_H_
void parseHeader(char* buffer, unsigned short *readnlength, char **readname, unsigned long *readfilelength);


//char *getSha1(char *buff, int bufferlength);

#endif

